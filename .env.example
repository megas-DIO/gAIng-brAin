# Server
PORT=3000

# Comma-separated tokens allowed to call Brain API
# Example: BRAIN_TOKENS=token1,token2
BRAIN_TOKENS=change-me

# Optional hardening for MCP DNS rebinding protection
# Comma-separated values
ALLOWED_HOSTS=adjoining-multimolecular-ursula.ngrok-free.dev,localhost:3000
ALLOWED_ORIGINS=https://chat.openai.com,https://claude.ai

# Supabase (server-side only)
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY

# Embeddings:
# Set to "none" for 100% free/no external calls (keyword search only)
# Set to "openai" to use vector search via match_memories RPC (requires OpenAI key/billing/credits)
EMBED_PROVIDER=none
OPENAI_API_KEY=
OPENAI_EMBED_MODEL=text-embedding-3-small
